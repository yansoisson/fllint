# Fllint Configuration
# Copy to .env and modify as needed

# Server port
FLLINT_PORT=8420

# Data directory (conversations, config, uploads)
FLLINT_DATA_DIR=./data

# Models directory (GGUF files)
FLLINT_MODELS_DIR=./models
